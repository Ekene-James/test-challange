# Vacination Summary API

## How To Use
* npm i : to install the dependencies
* npm run dev : runs it on local
* npm run build : builds the output file to a dist folder for production use
* npm start : starts the production app file

